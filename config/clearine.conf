[main]
    # set background opacity
    opacity = 0.2
    # set gaps
    gap-left = 70
    gap-right = 70
    gap-top = 70
    gap-bottom = 70
    # set mode (vertical/horizontal)
    mode = vertical
    # set spacing between card and widget
    spacing = 80

[command]
    # set command to launch when the button is clicked
    logout = bspc quit 1
    lock = i3lock-fancy -p -- scrot -z -o
    restart = systemctl reboot
    shutdown = systemctl poweroff

[shortcuts]
    cancel = Escape
    lock = k
    hibernate = H
    logout = l
    restart = r
    shutdown = s

[card]
    # set background color and border radius for card
    background-color = #5DAA79
    border-radius = 15
    # set padding
    padding-bottom = 10
    padding-left = 10
    padding-right = 10
    padding-top = 10

[button]
    # button theme name
    theme = Clearine-Fallback
    # button item sort
    items = logout, restart, shutdown, lock, cancel
    # set button text font and text color
    label-font = DejaVu Sans Book 9
    label-size = 14
    label-color = #101314
    # set button width and height
    width = 120
    height = 70
    # set button icon width and height
    icon-width = 32
    icon-height = 32
    # set per-button margin
    margin-bottom = 0
    margin-left = 0
    margin-right = 0
    margin-top = 0
    # set spacing between button
    spacing = 5
    # opacity
    opacity-normal = 0.7
    opacity-focus = 1.0

[widget]
    # set widget first line font, size, color and format
    firstline-font = DejaVu Sans ExtraLight
    firstline-size = 90
    firstline-color = #AC9B4D
    firstline-format = %H.%M
    # set widget second line font, size, color and format
    secondline-font = DejaVu Sans Book
    secondline-size = 14
    secondline-color = #e1e5e8
    secondline-format = %A, %d %B %Y
